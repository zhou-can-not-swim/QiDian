<!-- h: 命令弹窗内容：列出全部内置命令 -->
<UserControl x:Class="QiDian.Plugins.Help.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="360">
    <StackPanel MinWidth="300" MaxWidth="440">
        <TextBlock Text="内置命令"
                   FontSize="14" FontWeight="SemiBold" Foreground="#1F2937"/>
        <TextBlock Text="在搜索框中输入「前缀:」回车即可执行"
                   FontSize="12" Foreground="#6B7280" Margin="0,2,0,8"/>

        <ItemsControl ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 前缀徽标 -->
                        <Border Grid.Column="0"
                                Background="#E8F0FE" CornerRadius="4"
                                Padding="8,3" Margin="0,0,10,0"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding Prefix, StringFormat={}{0}:}"
                                       FontSize="12" FontWeight="SemiBold"
                                       Foreground="#1D4ED8"/>
                        </Border>

                        <!-- 标题 + 说明 -->
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Title}"
                                       FontSize="13" FontWeight="SemiBold"
                                       Foreground="#1F2937"/>
                            <TextBlock Text="{Binding Description}"
                                       FontSize="11" Foreground="#6B7280"
                                       Margin="0,2,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
